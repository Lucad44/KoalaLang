fun sqrt(num n) {
    if (n < 0) {
        return -1;
    }

    num guess = n / 2;
    num epsilon = 0.00000000001;

    while (guess ** 2 - number > epsilon || number - guess ** 2 > epsilon) {
        guess = (guess + number / guess) / 2;
    }

    return guess;
}
